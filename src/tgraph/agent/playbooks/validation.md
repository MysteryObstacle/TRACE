# Validation Playbook

Run validate before trusting a graph. Use inspect to focus on reported nodes, ports, links, or paths, then patch the graph only when the issue belongs to the IR.

Do not invent workflow fixes, knowledge, catalog entries, image choices, or domain intent while resolving validation issues.

