# Emission Playbook

Before emission, validate the graph. Use inspect if the target reports missing context. Phase one targets are placeholders; `emit` may return `target_not_implemented`.

Do not invent workflow, knowledge, catalog, image, flavor, provider, or domain decisions inside TGraph emission.

