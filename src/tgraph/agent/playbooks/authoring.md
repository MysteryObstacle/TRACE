# Authoring Playbook

Create or update a graph by using inspect for current context, patch for graph edits, and validate after every meaningful batch.

Do not invent workflow, knowledge, catalog, image, flavor, or domain assumptions inside TGraph. Put caller-owned context in the outer application.

