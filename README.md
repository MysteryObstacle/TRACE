# TRACE

TRACE is a staged agent runtime for turning user intent into topology artifacts.

## Quickstart

```bash
conda activate Trace
pip install -e .[dev]
pytest
python main.py run "PLC[1..2] with HMI1"
```
